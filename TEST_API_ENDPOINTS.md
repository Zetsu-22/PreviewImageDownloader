# 🔍 Как найти и протестировать API эндпоинты

## Метод 1: Использование DevTools браузера (Chrome/Firefox)

### Шаг 1: Откройте инструменты разработчика
1. Откройте сайт (например, https://kitsu.io)
2. Нажмите `F12` или `Ctrl+Shift+I` (Windows) / `Cmd+Option+I` (Mac)
3. Перейдите на вкладку **Network** (Сеть)

### Шаг 2: Выполните поиск на сайте
1. Введите поисковый запрос на сайте (например, "Attack on Titan")
2. Нажмите Enter или кнопку поиска

### Шаг 3: Найдите API запросы
1. В списке запросов найдите запросы к API
2. Обычно они имеют формат:
   - `/api/...`
   - `api.` в домене
   - JSON или XML в ответе

### Шаг 4: Изучите запрос
1. Кликните на запрос
2. Посмотрите вкладку **Headers**:
   - **Request URL** - это эндпоинт
   - **Request Method** - обычно GET или POST
   - **Query String Parameters** - параметры запроса
3. Посмотрите вкладку **Payload** (если POST запрос)
4. Посмотрите вкладку **Response** - формат ответа

### Шаг 5: Скопируйте эндпоинт
Скопируйте полный URL из Request URL и используйте его в приложении.

---

## Метод 2: Поиск в документации

### Популярные места для документации:
1. `/api/docs`
2. `/api/documentation`
3. `/developers`
4. `/api/v1/docs`
5. GitHub репозиторий проекта (часто в разделе docs/)

### Примеры:
- Kitsu: https://kitsu.io/api/edge (в описании API)
- Jikan: https://docs.api.jikan.moe/
- AniList: https://anilist.github.io/ApiV2-GraphQL-Docs/

---

## Метод 3: Тестирование эндпоинтов

### Вариант A: Использование curl (командная строка)

```bash
# Пример для Kitsu
curl "https://kitsu.io/api/edge/anime?filter[text]=naruto"

# Пример для Jikan
curl "https://api.jikan.moe/v4/anime?q=naruto&limit=5"

# С сохранением в файл
curl "https://kitsu.io/api/edge/anime?filter[text]=naruto" -o response.json
```

### Вариант B: Использование PowerShell (Windows)

```powershell
# Простой запрос
Invoke-WebRequest -Uri "https://kitsu.io/api/edge/anime?filter[text]=naruto" -Method GET

# С сохранением ответа
$response = Invoke-WebRequest -Uri "https://kitsu.io/api/edge/anime?filter[text]=naruto"
$response.Content | Out-File -FilePath "response.json"
```

### Вариант C: Использование Postman

1. Скачайте Postman: https://www.postman.com/downloads/
2. Создайте новый запрос (New Request)
3. Выберите метод (обычно GET)
4. Вставьте URL эндпоинта
5. Добавьте параметры в разделе Params
6. Нажмите Send
7. Изучите ответ

### Вариант D: Использование браузера

Просто вставьте URL в адресную строку браузера:
```
https://kitsu.io/api/edge/anime?filter[text]=naruto
```

Браузер покажет JSON ответ (если установлено расширение для форматирования JSON).

---

## Метод 4: Поиск в GitHub

### Поисковые запросы:
```
site:github.com "kitsu api" example
site:github.com "anilist api" csharp
site:github.com "jikan api" endpoint
```

### Что искать:
1. README файлы с примерами
2. Примеры кода в Issues
3. Готовые библиотеки и их исходный код
4. Документацию в репозиториях

---

## Метод 5: Анализ существующих библиотек

### Python библиотеки:
- `kitsu-py` - для Kitsu API
- `jikanpy` - для Jikan API
- `anilistpy` - для AniList API

Изучите исходный код этих библиотек, чтобы понять, какие эндпоинты они используют.

---

## 🧪 Быстрое тестирование эндпоинта в C#

Создайте простой тестовый метод:

```csharp
private async Task TestEndpoint(string url)
{
    try
    {
        using (HttpClient client = new HttpClient())
        {
            client.Timeout = TimeSpan.FromSeconds(30);
            HttpResponseMessage response = await client.GetAsync(url);
            response.EnsureSuccessStatusCode();
            string json = await response.Content.ReadAsStringAsync();
            
            // Выводим результат
            Console.WriteLine(json);
            // Или сохраняем в файл
            await File.WriteAllTextAsync("test_response.json", json);
        }
    }
    catch (Exception ex)
    {
        Console.WriteLine($"Ошибка: {ex.Message}");
    }
}
```

**Использование:**
```csharp
await TestEndpoint("https://kitsu.io/api/edge/anime?filter[text]=naruto");
```

---

## 📋 Чек-лист для проверки эндпоинта

- [ ] Эндпоинт возвращает JSON/XML
- [ ] Эндпоинт доступен без авторизации (или есть способ получить ключ)
- [ ] Эндпоинт поддерживает поиск по названию
- [ ] Эндпоинт возвращает URL превью изображений
- [ ] Эндпоинт не имеет слишком строгих ограничений (rate limiting)
- [ ] Эндпоинт стабильно работает
- [ ] Есть документация по эндпоинту

---

## 🔗 Полезные инструменты

1. **Postman** - https://www.postman.com/
   - Лучший инструмент для тестирования API

2. **Insomnia** - https://insomnia.rest/
   - Альтернатива Postman

3. **HTTPie** - https://httpie.io/
   - Командная строка для HTTP запросов

4. **JSON Formatter** (расширение браузера)
   - Удобно для просмотра JSON ответов

5. **DevTools** (встроено в браузер)
   - F12 для открытия

---

## 💡 Советы

1. **Всегда проверяйте документацию** - она часто обновляется
2. **Тестируйте в браузере сначала** - это самый быстрый способ
3. **Сохраняйте примеры ответов** - пригодится для разработки
4. **Проверяйте rate limits** - некоторые API имеют ограничения
5. **Используйте правильное кодирование URL** - `Uri.EscapeDataString()` в C#

---

**Последнее обновление:** 2025-01-27

