# 📚 Справочник API эндпоинтов для поиска аниме

Этот документ содержит список доступных API и их эндпоинтов для полноценного поиска аниме.

---

## 🎯 1. Kitsu API (уже используется)

**Документация:** https://kitsu.io/api/edge

**Базовый URL:** `https://kitsu.io/api/edge`

### Эндпоинты:

#### Поиск по тексту:
```
GET /anime?filter[text]={название}
```
**Пример:**
```
https://kitsu.io/api/edge/anime?filter[text]=Attack%20on%20Titan
```

#### Поиск с пагинацией:
```
GET /anime?filter[text]={название}&page[limit]={лимит}&page[offset]={смещение}
```
**Пример:**
```
https://kitsu.io/api/edge/anime?filter[text]=naruto&page[limit]=20&page[offset]=0
```

#### Фильтр по категориям:
```
GET /anime?filter[categories]={категория}
```

#### Фильтр по жанрам:
```
GET /anime?filter[genres]={жанр}
```

#### Получение одного аниме по ID:
```
GET /anime/{id}
```

**Преимущества:**
- ✅ Не требует API ключа
- ✅ Бесплатный
- ✅ Хорошая документация
- ✅ Поддерживает фильтры

**Недостатки:**
- ⚠️ Ограничение на количество результатов в одном запросе

---

## 🎯 2. Jikan API (MyAnimeList неофициальный API)

**Документация:** https://docs.api.jikan.moe/

**Базовый URL:** `https://api.jikan.moe/v4`

**Важно:** Это неофициальный API для MyAnimeList. Официальный API требует регистрации.

### Эндпоинты:

#### Поиск аниме:
```
GET /anime?q={название}&limit={лимит}
```
**Пример:**
```
https://api.jikan.moe/v4/anime?q=Attack%20on%20Titan&limit=10
```

#### Получение по ID:
```
GET /anime/{id}
```

#### Получение топовых аниме:
```
GET /top/anime?page={страница}&limit={лимит}
```

#### Поиск с фильтрами:
```
GET /anime?q={название}&type={тип}&status={статус}&rating={рейтинг}
```

**Параметры:**
- `type`: tv, movie, ova, special, ona, music
- `status`: airing, complete, upcoming
- `rating`: g, pg, pg13, r17, r, rx

**Преимущества:**
- ✅ Не требует API ключа
- ✅ Бесплатный
- ✅ Обширная база данных MyAnimeList
- ✅ Хорошая документация

**Недостатки:**
- ⚠️ Неофициальный API (может быть нестабильным)
- ⚠️ Ограничение: 3 запроса в секунду, 60 запросов в минуту

---

## 🎯 3. AniList API (GraphQL)

**Документация:** https://anilist.github.io/ApiV2-GraphQL-Docs/

**API Endpoint:** `https://graphql.anilist.co`

**Тип:** GraphQL API

### Пример запроса:

```graphql
query ($search: String) {
  Page(page: 1, perPage: 10) {
    media(search: $search, type: ANIME) {
      id
      title {
        romaji
        english
        native
      }
      coverImage {
        large
        extraLarge
      }
      description
      averageScore
      format
      status
      episodes
    }
  }
}
```

**HTTP запрос:**
```http
POST https://graphql.anilist.co
Content-Type: application/json

{
  "query": "query ($search: String) { Page(page: 1, perPage: 10) { media(search: $search, type: ANIME) { id title { romaji english } coverImage { large } } } } }",
  "variables": { "search": "Attack on Titan" }
}
```

**Преимущества:**
- ✅ Не требует API ключа
- ✅ Очень гибкий (GraphQL)
- ✅ Большая база данных
- ✅ Хорошая документация

**Недостатки:**
- ⚠️ Требует знания GraphQL
- ⚠️ Сложнее интегрировать

---

## 🎯 4. Shikimori API

**Документация:** https://shikimori.one/api/doc

**Базовый URL:** `https://shikimori.one/api`

**Примечание:** Русскоязычный сервис, может требовать регистрацию для некоторых эндпоинтов.

### Эндпоинты:

#### Поиск аниме:
```
GET /animes?search={название}&limit={лимит}
```
**Пример:**
```
https://shikimori.one/api/animes?search=Атака%20титанов&limit=10
```

#### Получение по ID:
```
GET /animes/{id}
```

#### Получение топовых:
```
GET /animes?kind=tv&status=released&order=ranked&limit=50
```

**Преимущества:**
- ✅ Русскоязычный интерфейс
- ✅ Хорошая база данных
- ✅ Поддержка русских названий

**Недостатки:**
- ⚠️ Может требовать авторизацию
- ⚠️ Документация на русском

---

## 🎯 5. MyAnimeList Official API

**Документация:** https://myanimelist.net/apiconfig/references/api/v2

**Регистрация:** Требуется создание приложения на https://myanimelist.net/apiconfig

**Базовый URL:** `https://api.myanimelist.net/v2`

### Эндпоинты:

#### Поиск аниме:
```
GET /anime?q={название}&limit={лимит}&fields=id,title,main_picture,synopsis
```
**Пример:**
```
https://api.myanimelist.net/v2/anime?q=Attack%20on%20Titan&limit=10&fields=id,title,main_picture,synopsis
```

**Заголовки:**
```
Authorization: Bearer {access_token}
```

**Преимущества:**
- ✅ Официальный API
- ✅ Большая база данных
- ✅ Надежный

**Недостатки:**
- ⚠️ Требует регистрации и OAuth2
- ⚠️ Сложнее в настройке

---

## 🎯 6. AniDB API

**Документация:** http://anidb.net/perl-bin/animedb.pl?show=api

**Базовый URL:** `http://api.anidb.net:9001/httpapi`

**Примечание:** Требует регистрации и получения client ID.

### Эндпоинты:

#### Поиск аниме:
```
GET /?request=anime&client={client}&clientver={version}&protover=1&request=anime&aid={anime_id}
```

**Преимущества:**
- ✅ Очень подробная информация
- ✅ Большая база данных

**Недостатки:**
- ⚠️ Требует регистрации
- ⚠️ Сложный API
- ⚠️ XML формат ответа

---

## 📝 Рекомендации по использованию

### Для начала (без регистрации):
1. **Kitsu API** - уже используется, хорошо работает
2. **Jikan API** - хорошая альтернатива с базой MyAnimeList
3. **AniList GraphQL** - если нужно больше гибкости

### Для продвинутого использования:
1. **MyAnimeList Official API** - если нужна официальная поддержка
2. **Shikimori API** - если нужна поддержка русских названий

### Комбинированный подход:
Можно использовать несколько API одновременно и объединять результаты для более полного покрытия.

---

## 🔧 Как найти эндпоинты самостоятельно

### 1. Проверка документации API
- Ищите официальную документацию на сайте сервиса
- Обычно находится в разделах: "API", "Developers", "Documentation"

### 2. Использование инструментов разработчика браузера
1. Откройте сайт (например, kitsu.io)
2. Нажмите F12 (DevTools)
3. Перейдите на вкладку "Network"
4. Выполните поиск на сайте
5. Найдите запросы к API в списке
6. Изучите URL и параметры запроса

### 3. Поиск в GitHub
- Ищите примеры использования: `site:github.com "kitsu api" example`
- Изучайте готовые библиотеки и их исходный код

### 4. Тестирование эндпоинтов
- Используйте Postman или curl для тестирования
- Проверяйте различные параметры запроса

---

## 🚀 Примеры интеграции

### Kitsu (уже используется):
```csharp
string apiUrl = $"https://kitsu.io/api/edge/anime?filter[text]={Uri.EscapeDataString(searchName)}";
```

### Jikan:
```csharp
string apiUrl = $"https://api.jikan.moe/v4/anime?q={Uri.EscapeDataString(searchName)}&limit=10";
```

### AniList GraphQL:
```csharp
// Требует POST запрос с JSON body
var query = new {
    query = "query($search: String) { Page { media(search: $search, type: ANIME) { id title { romaji } coverImage { large } } } } }",
    variables = new { search = searchName }
};
```

---

**Последнее обновление:** 2025-01-27

